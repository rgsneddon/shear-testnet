# p2p.shear.digital — validating seed hostname. No public pool, no public site.
server {
    listen 80;
    listen [::]:80;
    server_name p2p.shear.digital;
    root /var/www/p2p.shear.digital;
    index index.html;
    access_log /var/log/nginx/p2p.shear.digital.access.log shear_noqs;
    location ^~ /.well-known/acme-challenge/ {
        root /var/www/html;
        default_type text/plain;
    }
    location / {
        add_header Cache-Control "no-store";
        try_files $uri $uri/ /index.html;
    }
}
