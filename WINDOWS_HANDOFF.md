# Windows pointer — Shear

**Do not use this file as the pin list.** Status lives in:

**https://github.com/rgsneddon/handoff/blob/main/HANDOFF.md** (section **Shear**)

Laptop leftover for wallet **0.0.6** (no miner inside): **The Join** + 11-decimal SHE + vortice `vort1.` deploy keys. Mac-cut (dmg/apk/ios-unsigned) is **on tag** `v0.0.6`. Attach `-windows.zip` / `-linux.zip` / `-archlinux.zip` to **the same** `v0.0.6`. Official miner **0.1.2** stays on `v0.1.2` — **do not recut**. Do **not** recut wallet **0.0.5**. Testnet only. No mainnet zip. No sibling tag. Do **not** upload Shear to the App Store.
