# Windows pointer — Shear

**Do not use this file as the pin list.** Status lives in:

**https://github.com/rgsneddon/handoff/blob/main/HANDOFF.md** (section **Shear**)

Current pin: **wallet 0.0.3** (testnet only, no mainnet). Laptop leftover after the Mac ships Darwin:

1. Attach `shear-wallet-0.0.3-windows.zip` / `-linux.zip` / `-archlinux.zip` to **the same** `v0.0.3` tag. Put `shear-miner` / `shear-miner.exe` **0.1.1** **inside** those zips next to the GUI. Title Shear 0.0.3. Testnet `shear-testnet-v1`. she1 dests. Recut Mac packages already on the tag. No sibling tag. **No mainnet zip.**
2. Miner standalone `shear-miner-0.1.1-windows.zip` on **the same** `v0.1.1` leftover. Hash unchanged. `--user she1…`. Wallet zips still bundle the miner.
3. Do **not** attach Shear binaries to any other project’s tags. Do **not** invent sibling tags. Testnet first; do **not** cut a mainnet genesis zip.
