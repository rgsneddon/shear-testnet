# Windows pointer — Shear

**Do not use this file as the pin list.** Status lives in:

**https://github.com/rgsneddon/handoff/blob/main/HANDOFF.md** (section **Shear**)

Laptop leftover for wallet **0.0.8** (no miner inside): 1 SHE + 1e-11/hash, eight-digit public amounts, offer **she1**. Mac-cut (dmg/apk) is **on tag** `v0.0.8`. Mac DMG is Developer ID + notarized + stapled (clobbered onto the same tag). GitHub release title **`[TESTNET] Shear wallet 0.0.8`** — keep that prefix until mainnet. **No iOS / iPad zip** until App Store review. Attach `-windows.zip` / `-linux.zip` / `-archlinux.zip` to **the same** `v0.0.8`. Official miner pin is **0.1.5** on `v0.1.5` (5% dual-login fee). **0.1.4 is deprecated** (too slow) — **do not recut**. Do **not** recut miner **0.1.3** or wallet **0.0.7**. Testnet only. No mainnet zip. No sibling tag. Do **not** upload Shear to the App Store.
