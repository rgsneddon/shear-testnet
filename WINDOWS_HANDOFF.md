# Windows pointer — Shear

**Do not use this file as the pin list.** Leftover lives in:

**https://github.com/rgsneddon/handoff/blob/main/HANDOFF.md** (section **Shear**)

Current pin: **testnet 0.1.0** (no mainnet yet). Laptop leftover after the Mac ships Darwin:

1. `shear-miner-0.1.0-windows.zip` on the same tag as the Mac miner (`v0.1.0`). `--selftest` must print `selftest ok` with the ShearHash vector. **Feeless** — no dual-login. `client=ShearHash`. Default `--pool pool.shear.digital:1111`.
2. Later: `shear-wallet-0.1.0-windows.zip` / `-linux.zip` / `-archlinux.zip` with the miner **inside** the zip. Not this week if the wallet is not tagged yet.
3. Do **not** attach Shear binaries to any other project’s tags. Do **not** invent sibling tags. Testnet first; do **not** cut a mainnet genesis zip.
