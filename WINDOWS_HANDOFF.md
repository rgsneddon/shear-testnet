# Windows pointer — Shear

**Do not use this file as the pin list.** Status lives in:

**https://github.com/rgsneddon/handoff/blob/main/HANDOFF.md** (section **Shear**)

Laptop leftover for wallet **0.0.7** (no miner inside): 1 SHE + 1e-11/hash, eight-digit public amounts. Mac-cut (dmg/apk) is **on tag** `v0.0.7`. Mac DMG is Developer ID + notarized + stapled (clobbered onto the same tag). **No iOS / iPad zip** until App Store review. Attach `-windows.zip` / `-linux.zip` / `-archlinux.zip` to **the same** `v0.0.7`. Official miner **0.1.2** stays on `v0.1.2` — **do not recut**. Do **not** recut wallet **0.0.6**. Testnet only. No mainnet zip. No sibling tag. Do **not** upload Shear to the App Store.
