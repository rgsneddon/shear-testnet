# Windows pointer — Shear

**Do not use this file as the pin list.** Leftover lives in:

**https://github.com/rgsneddon/handoff/blob/main/HANDOFF.md** (section **Shear**)

Current pin: **wallet 0.0.1** (testnet only, no mainnet). Laptop leftover after the Mac ships Darwin:

1. Attach `shear-wallet-0.0.1-windows.zip` / `-linux.zip` / `-archlinux.zip` to **the same** `v0.0.1` tag. Put `shear-miner` / `shear-miner.exe` **inside** those zips next to the GUI. Title Shear 0.0.1. Testnet `shear-testnet-v1`. `shear1` addresses.
2. Standalone miner zip remains `shear-miner-0.1.0-windows.zip` on `v0.1.0` if that miner tag exists. Wallet zips still bundle the miner.
3. Do **not** attach Shear binaries to any other project’s tags. Do **not** invent sibling tags. Testnet first; do **not** cut a mainnet genesis zip.
