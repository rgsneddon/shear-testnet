# Windows pointer — Shear

**Do not use this file as the pin list.** Status lives in:

**https://github.com/rgsneddon/handoff/blob/main/HANDOFF.md** (section **Shear**)

Laptop leftover for wallet **0.0.9** (no miner inside) **on tag**: `shear-wallet-0.0.9-windows.zip` / `-linux.zip` / `-archlinux.zip` attached to **the same** `v0.0.9`. Continuum live pending miner hashes + mempool receives (`syncCredits`); spendable when sealed height advances. 9s blocks, 0.1 SHE pot + 1e-11/hash, eight-digit public amounts, offer **she1**. Tag **`v0.0.9`** (`92783e2`). `kWalletVersion` / `pubspec` **0.0.9**. Mac-cut (dmg/apk) is **not on tag yet** — Amelia cuts Developer ID + notarized + stapled DMG and release-signed APK onto **the same** `v0.0.9`. GitHub release title **`[TESTNET] Shear wallet 0.0.9`**. **No iOS / iPad zip.** Official miner pin is **0.1.7** on `v0.1.7` (5% dual-login fee, `she1qlrll…fee`). **Do not recut** **0.1.6**. **Do not recut** wallet **0.0.8**. Testnet only. No mainnet zip. No sibling tag. Do not upload Shear to the App Store.

Miner **0.1.7** leftover: Windows `shear-miner-0.1.7-windows.zip` (`shear-miner.exe` + `example.bat`) and `shear-miner-0.1.7-linux.zip`. Darwin zip is Mac-cut for Amelia onto **the same** `v0.1.7`. Sends this miner's own hashes/hashrate on each share. `--selftest` `6e95b903…`.
