# Windows pointer — Shear

**Do not use this file as the pin list.** Status lives in:

**https://github.com/rgsneddon/handoff/blob/main/HANDOFF.md** (section **Shear**)

Laptop leftover for wallet **0.1.0** (no miner inside): first-boot reconstructed spendable + one-block pending settle. Attach `shear-wallet-0.1.0-windows.zip` / `-linux.zip` / `-archlinux.zip` to **the same** `v0.1.0` after Mac-cut is on tag. Continuum loads confirmed SHE on unlock; open-round hashes/receives stay pending until the next sealed block. 9s blocks, 0.1 SHE pot + 1e-11/hash, eight-digit public amounts, offer **she1**. Tag **`v0.1.0`** (`401e000`). `kWalletVersion` / `pubspec` **0.1.0** (`0.1.0+10`). GitHub release title **`[TESTNET] Shear wallet 0.1.0`**. **No iOS / iPad zip.** Official miner pin is **0.1.7** on `v0.1.7`. Do **not** recut wallet **0.0.9**. Testnet only. No mainnet zip. No sibling tag. Do not upload Shear to the App Store.

Miner **0.1.7** leftover: Windows `shear-miner-0.1.7-windows.zip` (`shear-miner.exe` + `example.bat`) and `shear-miner-0.1.7-linux.zip`. Darwin zip is Mac-cut for Amelia onto **the same** `v0.1.7`. Sends this miner's own hashes/hashrate on each share. `--selftest` `6e95b903…`.
